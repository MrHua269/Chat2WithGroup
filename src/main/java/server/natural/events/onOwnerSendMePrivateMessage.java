package server.natural.events;

import me.albert.amazingbot.events.PrivateMessageEvent;
import org.bukkit.event.Listener;
import server.natural.Utils;
//todo 完成
public class onOwnerSendMePrivateMessage implements Listener {
     public void onOwnerSendMePrivateMessage(PrivateMessageEvent e){
         Utils.executor.execute(() ->{

         });
     }
}
